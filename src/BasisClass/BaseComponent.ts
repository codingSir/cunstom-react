export default class BaseComponent {
    /**
     * id
     * */
    id: string;

    /**
     * 组件的children
     * */
    childIds: any[] = [];

    /**
     * 布局类型
     * */
    layoutType: string
}